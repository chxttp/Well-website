import "../styles/Card.css"
function Card({text}) {
    return (  
        <div className="container">
            <div className="card-container">
    </div>  
        </div>
        
    );
}

export default Card;